datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"

}

model Produto {
  id     String  @id @default(uuid())
  nome   String
  preco  Float
  createdAt DateTime @default(now())
}
